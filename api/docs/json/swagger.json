{
  "openapi": "3.1.0",
  "info": {
    "title": "EZlion API Reference",
    "version": "0.1.0"
  },
  "paths": {
    "/api/v0/monsters": {
      "get": {
        "summary": "Get all monsters",
        "responses": {
          "200": {
            "description": "A list of monsters in the results key. Includes metadata key."
          },
          "400": {
            "description": "Return application/problem+json. Returns default value in default key."
          },
          "405": {
            "description": "Return application/problem+json. Returns default value in default key."
          },
          "500": {
            "description": "Return application/problem+json. Returns default value in default key."
          }
        }
      }
    },
    "/api/v0/monsters/{id}": {
      "get": {
        "summary": "Get a monster by ID",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": {
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A single monster"
          },
          "400": {
            "description": "Return application/problem+json. Returns default value in default key."
          },
          "404": {
            "description": "Return problem+json. Returns default value in default key."
          },
          "405": {
            "description": "Return application/problem+json. Returns default value in default key."
          },
          "500": {
            "description": "Return application/problem+json. Returns default value in default key."
          }
        }
      }
    }
  }
}
